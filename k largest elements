class Solution {
  public:
    vector<int> kLargest(vector<int>& arr, int k) {
    int n = arr.size();
       sort(arr.begin() , arr.end());
      
       vector<int>ans;
       for(int i = n-1;i>=n-k;i--){
           ans.push_back(arr[i]);
       }
       return ans;
    }
};
