//User function Template for C++


class Solution{
  public:
  long long int wineSelling(vector<int>& Arr, int N){
      long long ans=0;
      long long temp=0;
      for(int i=0;i<N;i++){
          temp=temp+Arr[i]; 
          ans=ans+abs(temp);
      }
      return ans;
  }

};
