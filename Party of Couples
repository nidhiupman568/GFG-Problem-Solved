class Solution{
    public:
    int findSingle(int n, int arr[]){
        sort(arr, arr+n);
        for(int i=0; i<n;){
            if(arr[i]!=arr[i+1]) return arr[i];
            else i = i+2;
        }
    }
};
