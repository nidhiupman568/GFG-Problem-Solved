class Solution {
  public:
    vector<int> optimalArray(vector<int> &arr) {
        // code here
        vector<int>res(1);
        for(int i=1;i<arr.size();i++){
            res.push_back(res[i-1]+arr[i]-arr[i/2]);
        }
        return res;
    }
};

