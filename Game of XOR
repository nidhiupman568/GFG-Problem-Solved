class Solution {
  public:
    int subarrayXor(vector<int>& arr) {
        // code here
        int n = arr.size(), ans = 0;
        for(int i = 0; i < n; i++) {
            if(((i + 1) * (n - i)) & 1) ans ^= arr[i];
        }
        return ans;
    }
};
